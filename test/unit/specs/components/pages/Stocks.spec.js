import Vue from 'vue';
import Stocks from '@/components/pages/Stocks';

describe('Stocks.vue', () => {
    it('should have a name', () => {
        expect(Stocks.name).to.equal('Stocks');
    });    

});